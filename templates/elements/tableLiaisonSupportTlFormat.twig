<!-- tableLiaisonSupportTlFormat.twig -->
<table class="table table-bordered table-hover table-condensed">
    <tr>
        <th>{% trans "Format" %}</th>
        <th>{% trans "Actif" %}</th>
        {#<th style="width: 7%;"></th>#}
    </tr>
    {% for i, data in data_table %}
    <tr>
        <td>
            {{ data.Nom }}<input type="hidden" id="IDPage_{{ data.ID }}" value="{{ data.ID }}">
        </td>
        <td class="text text-center">
            <input type="checkbox" id="chkFormatTlPage_{{ data.ID }}" {% if (data.IDSupport != '-' ) %} checked="checked" {% endif %} onchange="getLiaisonSupportTlFormat(this.checked?1:0,{{ data.ID }});">
        </td>
    </tr>
    {% endfor %}
</table>
{#{{ dump(data) }}#}