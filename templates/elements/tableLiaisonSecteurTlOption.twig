<!-- tableLiaisonSecteurTlOption.twig -->
<table class="table table-bordered table-hover table-condensed">
    <tr>
        <th>{% trans "Option" %}</th>
        <th>{% trans "Actif" %}</th>
        {#<th style="width: 7%;"></th>#}
    </tr>
    {% for i, data in data_table %}
    <tr>
        <td>{{ data.Nom }}</td>
        <input type="hidden" id="IDOption_{{ data.ID }}" value="{{ data.ID }}">
        <td class="text text-center">
            <input type="checkbox"
                   id="chkSecteurTlOption_{{ data.ID }}" {% if (data.IDSecteur != '-' ) %} checked="checked" {% endif %}
                   onchange="getLiaisonSecteurTlOption(this.checked?1:0,{{ data.ID }});">
        </td>
    </tr>
    {% endfor %}
</table>
{#{{ dump(data) }}#}